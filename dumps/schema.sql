-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.admin_config (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  platform_commission_percentage numeric NOT NULL DEFAULT 5.0,
  bcv_exchange_rate numeric NOT NULL DEFAULT 1,
  bcv_last_updated timestamp with time zone NOT NULL DEFAULT now(),
  auto_update_exchange_rate boolean NOT NULL DEFAULT false,
  stripe_public_key text,
  stripe_secret_key text,
  paypal_client_id text,
  zelle_email text,
  zelle_phone text,
  bank_account_bs text,
  crypto_wallet_address text,
  crypto_network text,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT admin_config_pkey PRIMARY KEY (id)
);
CREATE TABLE public.audit_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  admin_id uuid NOT NULL,
  action text NOT NULL,
  target_table text NOT NULL,
  target_id uuid,
  changes jsonb,
  reason text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT audit_logs_admin_id_fkey FOREIGN KEY (admin_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_comments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  user_id uuid,
  parent_comment_id uuid,
  content text NOT NULL,
  is_from_creator boolean DEFAULT false,
  is_anonymous boolean DEFAULT false,
  donor_name text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT campaign_comments_pkey PRIMARY KEY (id),
  CONSTRAINT fk_comment_campaign FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fk_comment_user FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT fk_comment_parent FOREIGN KEY (parent_comment_id) REFERENCES public.campaign_comments(id)
);
CREATE TABLE public.campaign_details (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL UNIQUE,
  full_story text NOT NULL,
  gallery_images ARRAY NOT NULL DEFAULT '{}'::text[],
  videos_urls ARRAY NOT NULL DEFAULT '{}'::text[],
  support_documents_urls ARRAY NOT NULL DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  support_documents ARRAY DEFAULT '{}'::text[],
  CONSTRAINT campaign_details_pkey PRIMARY KEY (id),
  CONSTRAINT campaign_details_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id)
);
CREATE TABLE public.campaign_followers (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  user_id uuid NOT NULL,
  notify_updates boolean DEFAULT true,
  notify_milestones boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT campaign_followers_pkey PRIMARY KEY (id),
  CONSTRAINT fk_follower_campaign FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fk_follower_user FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_guarantors (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  guarantor_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text,
  accepted_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT campaign_guarantors_pkey PRIMARY KEY (id),
  CONSTRAINT campaign_guarantors_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT campaign_guarantors_guarantor_id_fkey FOREIGN KEY (guarantor_id) REFERENCES public.guarantors(id)
);
CREATE TABLE public.campaign_reactions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  user_id uuid,
  ip_address inet,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT campaign_reactions_pkey PRIMARY KEY (id),
  CONSTRAINT fk_reaction_campaign FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fk_reaction_user FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_reports (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  reporter_id uuid,
  category USER-DEFINED NOT NULL,
  description text NOT NULL,
  status USER-DEFINED DEFAULT 'pending'::report_status,
  admin_notes text,
  reviewed_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  reviewed_at timestamp with time zone,
  CONSTRAINT campaign_reports_pkey PRIMARY KEY (id),
  CONSTRAINT fk_report_campaign FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fk_report_reporter FOREIGN KEY (reporter_id) REFERENCES public.users(id),
  CONSTRAINT fk_report_reviewer FOREIGN KEY (reviewed_by) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_shares (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  user_id uuid,
  platform text NOT NULL,
  ip_address inet,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT campaign_shares_pkey PRIMARY KEY (id),
  CONSTRAINT fk_share_campaign FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fk_share_user FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_updates (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  creator_id uuid NOT NULL,
  title text NOT NULL,
  content text NOT NULL,
  image_url text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT campaign_updates_pkey PRIMARY KEY (id),
  CONSTRAINT campaign_updates_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT campaign_updates_creator_id_fkey FOREIGN KEY (creator_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaigns (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  creator_id uuid NOT NULL,
  title text NOT NULL,
  description text NOT NULL,
  category text,
  location text,
  featured_image_url text,
  goal_amount_usd numeric NOT NULL,
  current_amount_usd numeric NOT NULL DEFAULT 0,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::campaign_status,
  rejected_reason text,
  days_duration integer NOT NULL DEFAULT 30,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  published_at timestamp with time zone,
  ended_at timestamp with time zone,
  is_featured boolean NOT NULL DEFAULT false,
  category_id uuid,
  story text,
  main_image_url text,
  slug text,
  urgency_level text DEFAULT 'medium'::text,
  view_count integer DEFAULT 0,
  share_count integer DEFAULT 0,
  comment_count integer DEFAULT 0,
  reaction_count integer DEFAULT 0,
  follower_count integer DEFAULT 0,
  donor_count integer DEFAULT 0,
  last_donation_at timestamp with time zone,
  is_verified boolean DEFAULT false,
  video_url text,
  beneficiary_name text,
  beneficiary_relationship text,
  risk_score integer CHECK (risk_score IS NULL OR risk_score >= 0 AND risk_score <= 100),
  reviewed_by uuid,
  review_notes text,
  reviewed_at timestamp with time zone,
  CONSTRAINT campaigns_pkey PRIMARY KEY (id),
  CONSTRAINT campaigns_creator_id_fkey FOREIGN KEY (creator_id) REFERENCES public.users(id),
  CONSTRAINT campaigns_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id),
  CONSTRAINT campaigns_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.users(id)
);
CREATE TABLE public.categories (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL UNIQUE,
  description text,
  icon_emoji text,
  order_index integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.donations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  donor_id uuid,
  email text NOT NULL,
  amount_usd numeric NOT NULL,
  amount_bs numeric,
  payment_method USER-DEFINED NOT NULL,
  payment_status USER-DEFINED NOT NULL DEFAULT 'pending'::payment_status,
  is_anonymous boolean NOT NULL DEFAULT false,
  donor_name text,
  stripe_payment_id text,
  paypal_transaction_id text,
  reference_number text,
  crypto_transaction_hash text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  approved_at timestamp with time zone,
  message text,
  tip_amount_usd numeric DEFAULT 0,
  admin_notes text,
  CONSTRAINT donations_pkey PRIMARY KEY (id),
  CONSTRAINT donations_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT donations_donor_id_fkey FOREIGN KEY (donor_id) REFERENCES public.users(id)
);
CREATE TABLE public.exchange_rates (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  rate numeric NOT NULL,
  raw_rate numeric NOT NULL,
  margin_percentage numeric NOT NULL DEFAULT 4.3,
  source text NOT NULL DEFAULT 'binance_p2p'::text,
  is_active boolean DEFAULT true,
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL,
  CONSTRAINT exchange_rates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.frozen_exchange_rates (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  session_id text NOT NULL,
  rate numeric NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL,
  used boolean DEFAULT false,
  donation_id uuid,
  CONSTRAINT frozen_exchange_rates_pkey PRIMARY KEY (id),
  CONSTRAINT frozen_exchange_rates_donation_id_fkey FOREIGN KEY (donation_id) REFERENCES public.donations(id)
);
CREATE TABLE public.fund_freezes (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  campaign_id uuid,
  frozen_amount_usd numeric NOT NULL,
  frozen_amount_bs numeric,
  reason text NOT NULL CHECK (reason = ANY (ARRAY['verification_rejected'::text, 'fraud_detected'::text, 'user_reported'::text, 'admin_action'::text, 'other'::text])),
  reason_details text,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'released'::text, 'refunded'::text])),
  frozen_by uuid NOT NULL,
  frozen_at timestamp with time zone NOT NULL DEFAULT now(),
  released_by uuid,
  released_at timestamp with time zone,
  release_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT fund_freezes_pkey PRIMARY KEY (id),
  CONSTRAINT fund_freezes_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT fund_freezes_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT fund_freezes_frozen_by_fkey FOREIGN KEY (frozen_by) REFERENCES auth.users(id),
  CONSTRAINT fund_freezes_released_by_fkey FOREIGN KEY (released_by) REFERENCES auth.users(id)
);
CREATE TABLE public.guarantors (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL UNIQUE,
  organization_name text,
  rif_number text,
  profession_field text,
  credential_document_url text,
  kyc_status USER-DEFINED NOT NULL DEFAULT 'pending'::kyc_status,
  kyc_rejected_reason text,
  biography text,
  verification_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT guarantors_pkey PRIMARY KEY (id),
  CONSTRAINT guarantors_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  type USER-DEFINED NOT NULL,
  title text NOT NULL,
  message text NOT NULL,
  link text,
  read boolean DEFAULT false,
  data jsonb,
  created_at timestamp with time zone DEFAULT now(),
  read_at timestamp with time zone,
  campaign_id uuid,
  related_id uuid,
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT fk_notification_user FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT notifications_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id)
);
CREATE TABLE public.payment_method_bank_accounts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  method_code text NOT NULL DEFAULT 'transfer'::text,
  bank_name text NOT NULL,
  account_holder text NOT NULL,
  account_number text NOT NULL,
  account_type text,
  document_id text,
  currency text NOT NULL DEFAULT 'BS'::text,
  instructions text,
  is_active boolean NOT NULL DEFAULT true,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT payment_method_bank_accounts_pkey PRIMARY KEY (id),
  CONSTRAINT payment_method_bank_accounts_method_code_fkey FOREIGN KEY (method_code) REFERENCES public.payment_method_configs(code)
);
CREATE TABLE public.payment_method_configs (
  code text NOT NULL CHECK (code = ANY (ARRAY['card'::text, 'crypto'::text, 'zelle'::text, 'pagomovil'::text, 'transfer'::text])),
  name text NOT NULL,
  description text,
  is_active boolean NOT NULL DEFAULT false,
  display_order integer NOT NULL DEFAULT 0,
  settings jsonb NOT NULL DEFAULT '{}'::jsonb,
  updated_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT payment_method_configs_pkey PRIMARY KEY (code),
  CONSTRAINT payment_method_configs_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.payment_transactions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  donation_id uuid NOT NULL,
  provider text NOT NULL,
  status text NOT NULL,
  amount_usd numeric NOT NULL,
  amount_bs numeric,
  external_id text,
  provider_data jsonb,
  error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  CONSTRAINT payment_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT payment_transactions_donation_id_fkey FOREIGN KEY (donation_id) REFERENCES public.donations(id)
);
CREATE TABLE public.user_suspensions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  suspension_type text NOT NULL CHECK (suspension_type = ANY (ARRAY['temporary'::text, 'permanent'::text])),
  reason text NOT NULL CHECK (reason = ANY (ARRAY['verification_rejected'::text, 'fraud'::text, 'tos_violation'::text, 'user_request'::text, 'other'::text])),
  reason_details text NOT NULL,
  suspended_at timestamp with time zone NOT NULL DEFAULT now(),
  suspended_until timestamp with time zone,
  lifted_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  suspended_by uuid NOT NULL,
  lifted_by uuid,
  lift_reason text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_suspensions_pkey PRIMARY KEY (id),
  CONSTRAINT user_suspensions_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT user_suspensions_suspended_by_fkey FOREIGN KEY (suspended_by) REFERENCES auth.users(id),
  CONSTRAINT user_suspensions_lifted_by_fkey FOREIGN KEY (lifted_by) REFERENCES auth.users(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL,
  email text NOT NULL UNIQUE,
  phone text,
  full_name text NOT NULL,
  avatar_url text,
  bio text,
  role USER-DEFINED NOT NULL DEFAULT 'donor'::user_role,
  kyc_status USER-DEFINED NOT NULL DEFAULT 'pending'::kyc_status,
  kyc_document_url text,
  kyc_document_type text,
  kyc_rejected_reason text,
  location text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  email_verified boolean DEFAULT false,
  phone_verified boolean DEFAULT false,
  notification_preferences jsonb DEFAULT '{"sms": false, "push": true, "email": true}'::jsonb,
  is_banned boolean DEFAULT false,
  ban_reason text,
  last_login_at timestamp with time zone,
  terms_version_accepted text,
  terms_accepted_at timestamp with time zone,
  kyc_doc_type text,
  kyc_doc_url text,
  verified_at timestamp with time zone,
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.verification_requests (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  verification_type text NOT NULL CHECK (verification_type = ANY (ARRAY['individual'::text, 'company'::text])),
  full_name text NOT NULL,
  document_type text NOT NULL CHECK (document_type = ANY (ARRAY['cedula'::text, 'rif'::text, 'passport'::text])),
  document_number text NOT NULL,
  birth_date date,
  nationality text,
  phone text NOT NULL,
  email text NOT NULL,
  address text NOT NULL,
  city text,
  state text,
  country text NOT NULL DEFAULT 'Venezuela'::text,
  postal_code text,
  company_name text,
  company_rif text,
  company_type text,
  company_registration_date date,
  document_front_url text NOT NULL,
  document_back_url text,
  selfie_url text NOT NULL,
  proof_of_address_url text,
  company_registration_url text,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'under_review'::text, 'approved'::text, 'rejected'::text, 'suspended'::text])),
  rejection_reason text,
  rejection_details text,
  reviewed_by uuid,
  reviewed_at timestamp with time zone,
  ip_address text,
  user_agent text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT verification_requests_pkey PRIMARY KEY (id),
  CONSTRAINT verification_requests_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT verification_requests_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.withdrawal_accounts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  creator_id uuid NOT NULL,
  account_type text NOT NULL,
  account_holder_name text NOT NULL,
  account_number text,
  phone_number text,
  ci_number text,
  bank_code text,
  zelle_email text,
  paypal_email text,
  crypto_wallet_address text,
  crypto_network text,
  is_primary boolean NOT NULL DEFAULT false,
  verified boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT withdrawal_accounts_pkey PRIMARY KEY (id),
  CONSTRAINT withdrawal_accounts_creator_id_fkey FOREIGN KEY (creator_id) REFERENCES public.users(id)
);
CREATE TABLE public.withdrawal_requests (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  creator_id uuid NOT NULL,
  campaign_id uuid,
  account_id uuid NOT NULL,
  amount_usd numeric NOT NULL,
  amount_bs numeric,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::withdrawal_status,
  exchange_rate_used numeric,
  reference_number text,
  rejection_reason text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  processed_at timestamp with time zone,
  CONSTRAINT withdrawal_requests_pkey PRIMARY KEY (id),
  CONSTRAINT withdrawal_requests_creator_id_fkey FOREIGN KEY (creator_id) REFERENCES public.users(id),
  CONSTRAINT withdrawal_requests_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT withdrawal_requests_account_id_fkey FOREIGN KEY (account_id) REFERENCES public.withdrawal_accounts(id)
);